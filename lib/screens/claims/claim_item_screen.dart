import 'package:flutter/material.dart';

class ClaimItemScreen extends StatelessWidget {
  const ClaimItemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Claim Item Screen'),
      ),
    );
  }
}
